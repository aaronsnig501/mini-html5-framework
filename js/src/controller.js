function AboutController() {
  var context = {
    pageTitle: "About"
  };

  return context;
}

function HomeController() {
  var context = {
    pagetitle: "Welcome",
    description: "the mini HTML5 JavaScript framework"
  };

  return context;
}

function DocumentationController() {
  var context = {
    pageTitle: "Documentation",
    description: "Non-existant for at the moment..."
  };

  return context;
}
